export class Inventory{constructor(){this.items=[];this.capacity=24}add(id,qty=1){const found=this.items.find(x=>x.id===id);if(found){found.qty+=qty;return true}if(this.items.length>=this.capacity)return false;this.items.push({id,qty});return true}remove(id,qty=1){const f=this.items.find(x=>x.id===id);if(!f||f.qty<qty)return false;f.qty-=qty;if(f.qty<=0)this.items=this.items.filter(x=>x!==f);return true}count(id){return this.items.find(x=>x.id===id)?.qty??0}serialize(){return this.items.map(x=>({...x}))}load(a){this.items=Array.isArray(a)?a.map(x=>({...x})):[]}}
