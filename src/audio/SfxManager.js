export class SfxManager{constructor(scene){this.scene=scene}beep(freq=440,duration=.08){const ctx=this.scene.sound?.context;if(!ctx)return;try{const o=ctx.createOscillator(),g=ctx.createGain();o.frequency.value=freq;g.gain.setValueAtTime(.03,ctx.currentTime);g.gain.exponentialRampToValueAtTime(.0001,ctx.currentTime+duration);o.connect(g);g.connect(ctx.destination);o.start();o.stop(ctx.currentTime+duration)}catch{}}pickup(){this.beep(880,.09)}hit(){this.beep(180,.06)}confirm(){this.beep(620,.08)}}
